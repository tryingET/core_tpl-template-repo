# 2026-03-13 — DSL learning linkage from architecture pattern

## What I Did
- Decided to keep the main architecture learning focused on the primary pattern: stable core + thin adapters + ports at real seams.
- Added a separate crystallized learning for when recurring operation languages inside the core should become explicit contracts / DSLs.
- Linked the main architecture learning to the new DSL learning instead of duplicating the argument.

## Why
- The DSL question is a real sub-pattern, but not the top-level architecture headline.
- Linking keeps the architecture learning readable and keeps the DSL formalization guidance reusable without repeating prose.

## Crystallization Candidates
- already crystallized directly into `docs/learnings/`
