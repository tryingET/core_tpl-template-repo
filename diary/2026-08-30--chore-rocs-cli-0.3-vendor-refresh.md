---
summary: "AK 5188 record for refreshing the embedded tpl-project-repo ROCS bundle to authoritative 0.3.0."
read_when:
  - "Reviewing the self-contained ROCS bundle provenance or L1/L2 fixture fanout."
type: "diary"
---

# AK 5188 — embedded rocs-cli 0.3.0 refresh

## Source correction

Initial controller wording named healthco as the upstream source. Fail-closed preflight proved that live healthco was stale schema-1 `0.1.4`, retained forbidden `gitlab.py`, and lacked newer L0 files; copying it would have been a regression. The controller recorded corrective evidence 7920 and authorized the true source: clean detached `core/rocs-cli` commit `ecd48bbbf79c3eb8c67726ff238b70320fd4551a` (`0.3.0`). Template-propagator corrected AK 5186 evidence/report and supplied commit `42ca1a9` as an independent byte reference.

## Execution

From a clean detached source worktree, ran the canonical `rocs vendor` flow with CPython 3.12 so the native self-contained runtime matches the verified cross-repo bundle deterministically. Dry-run preceded apply. The resulting schema-3 `VENDORED_HASHES.json` binds source commit `ecd48bb...`, upstream version `0.3.0`, the exact `uv.lock`, 580 regular files, and JCS bundle digest `d851899e...`.

The source receipt is byte-identical to template-propagator `42ca1a9`, and every receipt-declared file byte-compares equal. Generated bytecode/lock residue was removed before fixture synchronization. The canonical source was then rendered through all seven L1/L2/matrix fixture copies.

## Guardrails

L0 and generated-L1 checks now require:

- receipt schema 3;
- upstream version 0.3.0;
- authoritative source commit `ecd48bb...`;
- absence of both `gitlab.py` and `gitlab_ci.py`;
- standalone `vendored-check` success under isolated Python with bytecode disabled;
- no `__pycache__`, build, dist, or egg-info residue;
- byte-identical fixture receipts across all seven copies.

## Validation before commit

- Authoritative dry-run/apply: passed.
- Standalone `vendored-check`: passed.
- 580 declared files byte-match template-propagator reference.
- `bash ./scripts/check-l0-guardrails.sh`: passed.
- `bash ./scripts/check-l0-generation.sh`: passed.

## Rollback

Revert the scoped AK 5188 commit. No healthco, softwareco, core/rocs-cli, or template-propagator repository was mutated.
