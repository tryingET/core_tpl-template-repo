# Organization context (project-local snapshot)

This repository is a **project** L2 scaffold.

Use this file to keep the organization context required by the project team,
while treating org baseline governance as the upstream source.

Canonical organization source: not set.
Set `org_docs_canonical_ref` when this repository should explicitly track an external org source of truth.


## Working rule

- Keep this context synchronized with organization-level governance intent.
- Record local deviations in `docs/project/governance_overlay.md` when applicable.
