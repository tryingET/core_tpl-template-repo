# Organization operating model

This repository uses the **{{ org_docs_profile }}** organization docs profile.

{% if org_docs_canonical_ref %}Canonical organization source: {{ org_docs_canonical_ref }}
{% else %}Canonical organization source: not set.
Set `org_docs_canonical_ref` when this repository should explicitly track an external org source of truth.
{% endif %}

## Profile guidance

- `compact`: keep org docs lean in this repository and focus on project execution docs.
- `rich`: keep a full org docs pack in `docs/org/`.
