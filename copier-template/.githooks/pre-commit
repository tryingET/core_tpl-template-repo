#!/usr/bin/env sh
set -eu

repo_root="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
"$repo_root/scripts/ci/smoke.sh"
# Company-owned extension point (never touched by template refresh).
exec "$repo_root/scripts/lib/run-local-hook.sh" local/githooks/pre-commit "$@"
